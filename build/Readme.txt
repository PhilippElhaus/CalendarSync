CalendarSync QuickStart
1) Edit config.json with your iCloud URL, Apple ID, app password, PrincipalId, and WorkCalendarId.
2) Run CalendarSync.exe once to verify log output (sync.log) 
3) Register it as a Scheduled Task (At log on → run with highest privileges).
