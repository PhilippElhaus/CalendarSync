CalendarSync QuickStart
1) Copy config.example.json to config.json.
2) Edit config.json with your iCloud URL, Apple ID, app password, PrincipalId, and WorkCalendarId.
3) Run CalendarSync.exe once to verify log output (sync.log).
4) Register it as a Scheduled Task (At log on → run with highest privileges).
